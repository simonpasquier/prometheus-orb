description: >
  Golang container executor

parameters:
  version:
    description: Go version
    type: string

executors:
  golang:
    docker:
    - image: circleci/golang:<< parameters.version >>
