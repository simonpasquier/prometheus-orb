parameters:
  resource_class:
    type: string
    default: medium
  version:
    type: string

resource_class: << parameters.resource_class >>
docker:
  - image: circleci/golang:<< parameters.version >>
